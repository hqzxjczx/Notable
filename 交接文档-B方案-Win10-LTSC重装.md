# 交接文档 B：Windows 10 Enterprise LTSC 2021 全新重装
生成者：Hermes Agent（WSL2 内）  生成日期：2026-08-11
执行者：PowerShell / Windows 侧智能体（需管理员权限）
用途：本机 Windows 10 2004 旧系统 → 全新安装 LTSC 2021

============================================================
0. 目标机器实测信息（已核实，勿改动配置假设）
============================================================
  CPU    Intel Core i5-7300U（7 代低压双核 4 线程）
  内存   约 8GB
  磁盘   仅 C 盘 238GB（已用 210G，可用 28G）——存储吃紧
  系统   Windows 10 build 19041.7548（2004 基座，补丁至 2024 年中）
  显卡   核显 HD 620（无独立 GPU）
  激活   当前为数字许可证（重装 LTSC 不适用，见阶段 3 警告）
  WSL    发行版 Ubuntu-22.04，vhdx 位于：
         C:\Users\hqzxj\AppData\Local\wsl\{5f63416f-1427-49b5-9556-792dc65a0116}\ext4.vhdx（8.7G）
         另一闲置发行版 vhdx（11.4G）：
         C:\Users\hqzxj\AppData\Local\wsl\{aced8343-0dbe-4542-b39d-64480e509b9a}\ext4.vhdx
         Hermes 数据 ~/.hermes 约 3.7G（在 WSL 发行版内，含记忆/技能/配置）

============================================================
1. 为什么选 LTSC 2021（决策依据，供用户复核）
============================================================
  - i5-7300U 不在 Win11 官方支持列表（需 8 代+），Win11 24H2 强装体验差
  - 普通 Win10 22H2 已于 2025-10-14 停止安全更新（EOL）
  - LTSC 2021 支持到 2027-01-12；IoT 企业版 LTSC 2021 支持到 2032-01-13
  - LTSC 无商店/Cortana/少预装，全新安装占用约 20-25G，对存储吃紧最友好
  ★ 重要警告：LTSC 需要批量许可（MAK/KMS/企业授权）激活，个人用户无
    免费数字许可证。激活失败/无密钥时的回退方案见第 8 节（改装 22H2
    专业版）。执行前必须与用户确认密钥来源，否则先停在这里。

============================================================
2. 阶段 0 备份（不做完不许进阶段 1）
============================================================
  2.1 个人文件 → 外接盘（先确认外接盘空间 ≥ 60G）
      robocopy C:\Users\hqzxj\Desktop  D:\backup\Desktop  /E /R:1 /W:1
      robocopy C:\Users\hqzxj\Documents D:\backup\Documents /E /R:1 /W:1
      robocopy C:\Users\hqzxj\Downloads D:\backup\Downloads /E /R:1 /W:1
      （图片/视频等目录同理；AppData 可不备，多为缓存）
  2.2 WSL 发行版备份（Hermes 全家在里头，二选一，推荐 a）：
      a) vhdx 文件级备份（最完整，含 Hermes 一切）：
         wsl --shutdown
         copy "C:\Users\hqzxj\AppData\Local\wsl\{5f63416f-1427-49b5-9556-792dc65a0116}\ext4.vhdx" D:\backup\ubuntu-22.04.vhdx
         （若另一发行版也需保留，同样复制 {aced8343-...} 的 vhdx）
      b) 或 wsl --export Ubuntu-22.04 D:\backup\ubuntu-22.04.tar
         （注意：import 恢复后 sparse 设置、vhdx 路径等元数据会丢失）
  2.3 记录笔记本品牌型号（驱动下载用）：
      wmic csproduct get name,vendor
  2.4 核验当前激活状态（英文界面对照，本机界面为英文）：
      设置→Update & Security→Activation，看 Activation status 行：
        "Windows is activated with a digital license" = 数字许可证（重装 22H2 自动激活；LTSC 不适用）
        "...linked to your Microsoft account"          = 数字许可证+微软账户（同上）
        "Windows is activated with a product key"      = 密钥激活（重装同版本自动激活）
        "...your organization's activation service"    = 企业批量激活（VOLUME channel）
        "Windows isn't activated"                      = 未激活
      或管理员 PowerShell 精确核验（返回 1 = 已激活）：
        (Get-CimInstance -ClassName SoftwareLicensingProduct -Filter
          "PartialProductKey IS NOT NULL AND ApplicationID=
           '55c92734-d682-4d71-983e-d6ec3f16059f'").LicenseStatus
      再看渠道：slmgr /dli → RETAIL/OEM channel = 数字权利，重装同版本
      自动激活；VOLUME channel = 批量授权渠道 —— 若为 VOLUME，本方案
      LTSC 激活无障碍（沿用现有批量渠道）；否则 LTSC 需另备 MAK/KMS
      授权，见阶段 3 警告
      ★ 2026-08-11 本机核验结果（已确认，勿重复执行）：
        LicenseStatus = 1（已激活）｜渠道 OEM_DM（品牌机出厂数字标记，
        永久激活，绑定主板）｜版本 Windows 10 Professional
        → 非 VOLUME 渠道，无批量授权；LTSC 需另备 MAK/KMS 或评估版
        （90 天试用），阶段 3 警告成立；装 LTSC 不消耗 OEM 权利，
        随时可回 22H2 专业版自动激活（见第 8 节）
  2.5 浏览器书签导出（Edge/Chrome 设置里导出 HTML）、密码管理器导出
  2.6 记录已购软件/许可证（Office 密钥等）
  ☐ 验证：外接盘上能看到上述文件，vhdx 大小与 8.7G 一致

============================================================
3. 阶段 1 制作安装介质
============================================================
  3.1 获取 LTSC 2021 ISO（二选一）：
      a) 微软评估中心 Microsoft Evaluation Center 搜索
         "Windows 10 Enterprise LTSC 2021" 下载评估版 ISO
         （90 天试用，合法免费；长期使用需激活，见阶段 3）
      b) 若用户持有批量许可（VLSC），从 VLSC 下载正式 ISO
  3.2 Rufus 写入 U 盘（≥8G）：
      - 分区类型选 GPT，目标系统 UEFI（7300U 平台默认）
      - 文件系统 NTFS
  3.3 备好笔记本官网驱动包（或装完联网更新，见阶段 4）

============================================================
4. 阶段 2 执行安装
============================================================
  4.1 重启进 BIOS/UEFI（F2/F10/F12/Del），U 盘设第一启动项
  4.2 安装向导：语言中文 → 现在安装 → 密钥页选"我没有产品密钥"
      （LTSC 评估版镜像无密钥页则直接下一步）
  4.3 版本选择：Windows 10 Enterprise LTSC（镜像内唯一 LTSC 选项）
  4.4 接受许可 → 自定义安装
  4.5 分区页：
      ★★★ 删除磁盘上所有分区（此操作不可逆，本机只有 C 盘，
      数据已备份则放心删；确认无误再点删除）★★★
      → 选择"未分配空间"→ 下一步（自动建 EFI/系统/恢复分区）
  4.6 等待安装完成，自动重启数次，进入 OOBE：
      - 区域中国、键盘微软拼音
      - 账户建议本地账户（LTSC 无商店，微软账户收益小）
      - 关闭所有"诊断数据/广告 ID"类开关（用户隐私敏感）

============================================================
5. 阶段 3 激活（LTSC 特有步骤，务必先与用户确认）
============================================================
  5.1 激活方式（按用户持有的授权任选）：
      a) MAK 密钥：设置→激活→更改产品密钥 输入
      b) KMS：slmgr /ipk <KMS客户端密钥>  +  slmgr /skms <KMS服务器>  +  slmgr /ato
         （KMS 客户端密钥：LTSC 2021 通用 GVLK 需从微软官方文档获取）
      c) 评估版：无需激活，90 天试用
  5.2 验证：slmgr /xpr  → 应显示激活状态
  ★ 若用户没有合法授权且不愿用评估版：停止本方案，转执行
    "交接文档-D方案" 第 8 节（改装 22H2 专业版，数字许可证自动激活）

============================================================
6. 阶段 4 驱动与系统更新
============================================================
  6.1 设备管理器检查感叹号设备；缺驱动去笔记本厂商官网按
      2.3 记录的型号下载（重点：Intel 核显、网卡、声卡、触控板）
  6.2 Windows Update 打补丁（LTSC 只收安全更新，量小）
  6.3 恢复数据：把 D:\backup 内容复制回原目录

============================================================
7. 阶段 5 恢复 WSL 与 Hermes（用户日常主力环境）
============================================================
  7.1 启用 WSL：
      wsl --install --no-distribution
      重启后：
      wsl --install -d Ubuntu-22.04   （先装同名发行版，仅用于占位）
      或直接导入备份（推荐，一步到位）：
      wsl --import Ubuntu-22.04 C:\WSL\ubuntu-22.04 D:\backup\ubuntu-22.04.tar --version 2
  7.2 若用了 vhdx 文件备份（2.2-a）：
      wsl --shutdown
      把 D:\backup\ubuntu-22.04.vhdx 覆盖回原路径
      C:\Users\hqzxj\AppData\Local\wsl\{5f63416f-...}\ext4.vhdx
      （需先卸载同名占位发行版，或直接 wsl --import 指定新 vhdx 路径）
  7.3 验证 Hermes：
      wsl -d Ubuntu-22.04
      hermes --version          （应显示 v0.20.0）
      ls ~/.hermes/memories     （记忆文件在）
      hermes                     （会话能起）
  7.4 恢复 /etc/wsl.conf 隔离配置（若 import 后丢失）：
      内容：systemd=true；[automount] enabled=false；hostname=secure-sandbox
      （参考原配置，改完 wsl --shutdown 重启生效）
  7.5 清理：删除闲置发行版 vhdx（{aced8343-...}，11.4G）释放空间：
      wsl --unregister <该发行版名>   （先 wsl -l -v 确认名字，别删错 22.04）

============================================================
8. 回滚/降级预案（LTSC 激活失败时）
============================================================
  改装 Windows 10 22H2 专业版（即交接文档 D 第 8 节 D-b 路径）：
  - 微软官方媒体创建工具下载 22H2 ISO（非 LTSC）
  - 安装时选"专业版"，密钥页选"我没有产品密钥"
  - 装完联网自动激活（原数字许可证绑定主板，自动识别）
  - 其余步骤（驱动/WSL 恢复）与本文档 6、7 节相同

============================================================
9. 完成验证清单（全部 ☑ 才算完成）
============================================================
  ☐ 系统信息显示 Windows 10 Enterprise LTSC 2021，build 19044.x
  ☐ 激活状态正常（slmgr /xpr 无过期提示；评估版除外）
  ☐ C 盘可用空间 ≥ 150G（全新系统 + 恢复数据后）
  ☐ 设备管理器无感叹号
  ☐ 网络正常
  ☐ wsl -l -v 能看到 Ubuntu-22.04 且 hermes --version 正常
  ☐ ~/.hermes 记忆/技能文件齐全（记忆、技能、配置都在）

============================================================
10. 执行原则（对所有阶段的硬性要求）
============================================================
  - 每一步执行前，先向用户说明"即将做什么、影响什么"
  - 破坏性操作（删除分区/卸载/删除目录）执行前二次确认
  - 每完成一个阶段，按该阶段 ☐ 项验证后再进下一阶段
  - 任何报错：记录完整错误信息，停止执行，向用户报告，不要猜测绕过
  - 全程不要碰注册表清理/优化工具/第三方"系统优化"软件
