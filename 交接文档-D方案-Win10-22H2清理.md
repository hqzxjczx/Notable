# 交接文档 D：保持 Windows 10 22H2（深度清理为主，可选重装 22H2）
生成者：Hermes Agent（WSL2 内）  生成日期：2026-08-11
执行者：PowerShell / Windows 侧智能体（需管理员权限）
用途：不换 Windows 大版本，先深度清理（D-a），空间仍不够再重装 22H2（D-b）

============================================================
0. 目标机器实测信息（已核实）
============================================================
  CPU    Intel Core i5-7300U | 内存 约 8GB | 仅 C 盘 238G（可用 28G）
  系统   Windows 10 build 19041.7548（2004 基座）
  激活   数字许可证（重装同版本自动激活，无需密钥）
  WSL    发行版 Ubuntu-22.04（vhdx 8.7G，Hermes ~/.hermes 3.7G 在其中）
         另一闲置发行版 vhdx 11.4G：
         C:\Users\hqzxj\AppData\Local\wsl\{aced8343-0dbe-4542-b39d-64480e509b9a}\ext4.vhdx
         swap.vhdx 残留（可删）：
         C:\Users\hqzxj\AppData\Local\Temp\F0F48AAA-B2F4-4414-84D6-78D6EA0F8A35\swap.vhdx（0.8G）
         C:\Users\hqzxj\AppData\Local\Temp\B73B9DC0-26FE-48AB-BF0E-7C964232696E\swap.vhdx（0G）
  C 盘根目录可疑项：hiberfil.sys / pagefile.sys / swapfile.sys（系统文件，
        不要动，见 4.3/4.4）、MSOCache、QMDownload、kingsoft、qqpcmgr_docpro、
        Python27、Python37、Exercism、new_tdx64、new_tdx_mock、pool、data、shell

============================================================
1. 决策说明
============================================================
  D-a = 不重装，深度清理 + 系统修复（零风险，预计回收 30-70G，先做）
  D-b = 重装回 Win10 22H2 专业版（数字许可证自动激活；仅 D-a 后空间
        仍不够时执行）
  普通 Win10 22H2 已于 2025-10-14 EOL（无免费安全更新），故本方案定位
  "过渡方案"：清理后保持可用，同时用户可考虑后续迁移（见文末备注）

============================================================
2. D-a 阶段 0 备份（10 分钟，必做）
============================================================
  2.1 关键文件 → 外接盘：
      robocopy C:\Users\hqzxj\Desktop  D:\backup\Desktop  /E /R:1 /W:1
      robocopy C:\Users\hqzxj\Documents D:\backup\Documents /E /R:1 /W:1
      robocopy C:\Users\hqzxj\Downloads D:\backup\Downloads /E /R:1 /W:1
  2.2 确认数字许可证（英文界面对照，本机系统界面为英文）：
      设置→Update & Security→Activation，看 Activation status 行：
        "Windows is activated with a digital license" = 数字许可证 ✓（重装自动激活）
        "...linked to your Microsoft account"          = 数字许可证+微软账户（同样自动激活）
        "Windows is activated with a product key"      = 密钥激活（重装后同样自动激活）
        "...your organization's activation service"    = 企业批量激活（重装后需重配，不能自动）
        "Windows isn't activated"                      = 未激活
      或管理员 PowerShell 精确核验（返回 1 = 已激活）：
        (Get-CimInstance -ClassName SoftwareLicensingProduct -Filter
          "PartialProductKey IS NOT NULL AND ApplicationID=
           '55c92734-d682-4d71-983e-d6ec3f16059f'").LicenseStatus
      再看渠道：slmgr /dli → RETAIL/OEM channel = 重装自动激活；
      VOLUME channel = KMS 批量，重装后需重新配置
      ★ 2026-08-11 本机核验结果（已确认，勿重复执行）：
        LicenseStatus = 1（已激活）｜渠道 OEM_DM（OEM 数字标记，
        品牌机出厂写入主板，永久激活）｜版本 Windows 10 Professional
        → 重装 22H2 专业版后自动激活，无需密钥；本机版本锁定"专业版"
  2.3 浏览器书签导出、密码管理器导出

============================================================
3. D-a 阶段 1 空间侦察（先看清 210G 花在哪）
============================================================
  3.1 下载 WizTree（https://wiztree.com，免费便携版）→ 右键"以管理员
      身份运行" → 选 C 盘 → 按大小排序，记录 Top 目录
  3.2 备选（无 GUI 时）：PowerShell 逐级统计：
      Get-ChildItem C:\ -Directory -ErrorAction SilentlyContinue |
        ForEach-Object { $s=(Get-ChildItem $_.FullName -Recurse -File
        -ErrorAction SilentlyContinue | Measure-Object Length -Sum).Sum;
        [PSCustomObject]@{Dir=$_.Name; GB=[math]::Round($s/1GB,2)} } |
        Sort-Object GB -Descending
      （慢，耐心等；WizTree 秒级，优先 WizTree）
  ☐ 验证：已知 210G 的主要去向后再动手

============================================================
4. D-a 阶段 2 系统级清理（管理员 PowerShell/CMD）
============================================================
  4.1 磁盘清理（含系统文件）：
      cleanmgr /d C:
      → 勾选：Windows 更新清理、临时文件、缩略图、旧 Windows 安装
      （有 Windows.old 则勾"以前的 Windows 安装"）
  4.2 关闭休眠（删 hiberfil.sys，约 3G+；用户不用休眠/快速启动时执行）：
      powercfg /h off
  4.3 系统还原点清理（不需要还原点时）：
      设置→系统→系统保护→C 盘→配置→删除
  4.4 页面文件压缩（8G 内存，上限 8G 够用；不要设 0）：
      wmic computersystem set AutomaticManagedPagefile=False
      wmic pagefileset where "name='C:\\pagefile.sys'" set InitialSize=4096,MaximumSize=8192
      重启生效
  4.5 系统健康检查（按顺序，每步等完成）：
      DISM /Online /Cleanup-Image /RestoreHealth
      sfc /scannow
      chkdsk C: /f    （提示重启时执行，重启后自动跑）
  ☐ 验证：C 盘可用空间应有明显增加（预计 +10~25G）

============================================================
5. D-a 阶段 3 软件级清理（本机大头，逐个确认后执行）
============================================================
  5.1 卸载（控制面板 appwiz.cpl 或 设置→应用，逐个卸载）：
      - QQ 电脑管家（qqpcmgr_docpro）
      - kingsoft 相关（金山/WPS 残留，WPS 若在用则保留并改缓存路径）
      - Python27 / Python37（先确认无程序依赖；有疑问就保留）
      - Exercism（不用则卸）
      - 其他 WizTree 扫出的久未使用软件（向用户确认后卸）
  5.2 删除目录（确认无用后删）：
      C:\MSOCache              （Office 安装缓存，1-3G；Office 已装好即可删）
      C:\QMDownload            （QQ 下载目录，确认无需要文件）
      C:\Windows\Temp\*        （系统临时文件）
      %TEMP%\*                 （用户临时文件）
      浏览器缓存（Edge/Chrome 设置里清除）
  5.3 根目录可疑项检查清单（先看内容再决定，禁止盲删）：
      new_tdx64 / new_tdx_mock —— 疑似通达信行情/交易软件（tdx），
          若用户仍在炒股用，保留安装目录，只清它的缓存目录；卸载走官方卸载
      pool / data / shell —— 先 dir 看内容，向用户确认来源后再处理
      inetpub —— IIS 默认目录，一般空壳，确认无站点后可不理
      Exercises/Exercism 相关目录 —— 随 5.1 卸载后清理
  5.4 国产软件缓存改道：WPS/QQ/微信等设置里把缓存/文件存储改到
      非系统盘（本机无 D 盘则改为外接盘或接受现状，勿强求）

============================================================
6. D-a 阶段 4 WSL 瘦身（本机可回收 10-20G，重点）
============================================================
  6.1 列出所有发行版，确认哪个闲置：
      wsl -l -v
      （当前主力是 Ubuntu-22.04，8.7G 那个；11.4G 的 {aced8343-...}
      很可能是闲置的 Ubuntu 24.04 或旧备份——先 wsl -d <名字> 进去
      看内容确认，未确认前不要删）
  6.2 确认闲置的发行版 → 整个删除回收空间：
      wsl --unregister <发行版名>
      ★ 删除不可逆，unregister 前再次确认该发行版内无需要数据
  6.3 主力发行版转稀疏（一劳永逸，之后 vhdx 不再虚涨）：
      wsl --shutdown
      wsl --manage Ubuntu-22.04 --set-sparse true
      （转换需几分钟，等待完成提示；完成后 wsl 自动可用）
      ★ 若 wsl --manage 不可用（老版本 WSL），改用 diskpart compact：
         wsl --shutdown
         diskpart
           select vdisk file="C:\Users\hqzxj\AppData\Local\wsl\{5f63416f-1427-49b5-9556-792dc65a0116}\ext4.vhdx"
           attach vdisk readonly
           compact vdisk
           detach vdisk
           exit
  6.4 删 swap.vhdx 残留（WSL 已 shutdown 状态下）：
      del "C:\Users\hqzxj\AppData\Local\Temp\F0F48AAA-B2F4-4414-84D6-78D6EA0F8A35\swap.vhdx"
      rmdir /s /q "C:\Users\hqzxj\AppData\Local\Temp\F0F48AAA-B2F4-4414-84D6-78D6EA0F8A35"
      （B73B9DC0 那个 0G 的同样处理）
  6.5 WSL 内部清理（2026-08-11 已由 WSL 侧智能体执行部分，勿重复）：
      [已执行] npm 缓存 470M→18M（npm cache clean --force）
      [已执行] uv 缓存 882M 清空（uv cache clean）
      [已执行] Firefox/Electron/node-gyp/pip 缓存 ~330M（~/.cache/ 下）
      [已执行] git 仓库压缩 .git 479M→415M（git gc）
      [已执行] WSL / 分区 9.6G→8.7G
      [待执行·需 sudo] 以下三条由用户在 WSL 终端跑（输入密码）：
        sudo apt-get clean                  # apt 缓存 246M
        sudo journalctl --vacuum-size=100M  # 日志 808M→100M
        sudo fstrim -v /                    # TRIM 标记删除块
      [禁止删除] ms-playwright 646M（Hermes browser 工具依赖）、
        ~/.hermes 下 node_modules/venv/apps/skills（运行必需）
      ★ 关键联动：WSL 内部腾出的空间要两步才从 C 盘消失——
        ① 用户跑 sudo fstrim -v /（标记删除块）
        ② 本机执行 wsl --shutdown + wsl --manage Ubuntu-22.04
           --set-sparse true（vhdx 物理收缩，C 盘回收 2~3G）
        顺序不可反：不 fstrim 直接 set-sparse，已删块的回收不彻底
  ☐ 验证：wsl -l -v 正常；wsl -d Ubuntu-22.04 能进；C 盘空间增加

============================================================
7. D-a 阶段 5 收尾验证
============================================================
  ☐ 重启一次（让 chkdsk/pagefile 设置生效）
  ☐ C 盘可用空间：预期从 28G → 60-100G+
  ☐ 常用软件全部能正常打开
  ☐ hermes 会话正常（wsl -d Ubuntu-22.04 → hermes）
  ☐ 若仍不足 40G 可用 → 进第 8 节 D-b 重装

============================================================
8. D-b 重装回 Win10 22H2（可选，仅 D-a 后仍不够时）
============================================================
  8.1 备份（与第 2 节相同 + WSL 导出）：
      wsl --shutdown
      wsl --export Ubuntu-22.04 D:\backup\ubuntu-22.04.tar
      或复制 vhdx：
      copy "C:\Users\hqzxj\AppData\Local\wsl\{5f63416f-...}\ext4.vhdx" D:\backup\
  8.2 制作介质：微软官网 software-download/windows10 → 媒体创建工具
      → ISO 或直接写 U 盘（≥8G）
  8.3 安装：U 盘引导 → 版本选"专业版"（或与当前一致）→ 密钥页选
      "我没有产品密钥" → 自定义安装 → 删除所有分区 → 下一步
      （本机只有 C 盘，数据已备份；删除分区不可逆，二次确认）
  8.4 激活：装完联网自动激活（数字许可证绑定主板），验证：
      slmgr /xpr
  8.5 恢复：驱动（厂商官网按型号）→ Windows Update → 数据复制回
  8.6 恢复 WSL/Hermes：
      wsl --import Ubuntu-22.04 C:\WSL\ubuntu-22.04 D:\backup\ubuntu-22.04.tar --version 2
      wsl -d Ubuntu-22.04 → hermes --version → ls ~/.hermes/memories
      恢复 /etc/wsl.conf（systemd=true；[automount] enabled=false；
      hostname=secure-sandbox；改完 wsl --shutdown 重启生效）
  8.7 释放闲置发行版：wsl -l -v 确认后 wsl --unregister 删除

============================================================
9. 危险区与注意事项（违反=事故）
============================================================
  - 禁止：pagefile 设为 0、关闭 UAC、修改注册表优化项、第三方清理大师
  - 禁止：删除 Program Files / Program Files (x86) / Windows / ProgramData
  - 禁止：wsl --unregister Ubuntu-22.04（主力发行版，含 Hermes）
  - 禁止：删除 new_tdx64 前不确认（疑似在用行情软件）
  - 任何"不确定有没有用"的文件/目录：先问用户，不要猜
  - 22H2 已 EOL：本方案是过渡；长期安全建议后续评估 LTSC（见交接文档 B）
    或 Linux 双系统（本机存储紧张，暂不推荐）

============================================================
10. 执行原则（硬性要求）
============================================================
  - 每步执行前向用户说明"做什么、影响什么"
  - 破坏性操作（删除/卸载/unregister）执行前二次确认
  - 每阶段按 ☐ 验证后再进下一阶段
  - 报错即停：记录完整错误信息，向用户报告，不猜测绕过
